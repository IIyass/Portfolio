import styled from "styled-components"
export const Wrapper = styled.header`
  ul {
    list-style: none;
    display: flex;
    flex-direction: row;
    margin: 0;
    padding: 0;
    li {
      padding-right: 2rem;
    }
  }
`
